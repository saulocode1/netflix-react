import styled from "styled-components";

const HeaderNetflix = styled.header`
    padding: 0.42rem 3.4rem;
    background-image: linear-gradient(0deg, transparent, #1414149c);
    display: flex;
    align-items: center;
`;

export default HeaderNetflix;
